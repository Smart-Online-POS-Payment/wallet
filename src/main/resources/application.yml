spring:
  application.name: wallet
  datasource:
    url: jdbc:postgresql://${database.endpoint}/${database.name}
    username: ${database.user}
    password: ${database.password}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
database:
  endpoint: 34.28.23.25:5432
  name: postgres
  user: postgres
  password: KP^FOz63OneNl{dH
server:
  port: 8082